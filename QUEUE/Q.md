1.init_queue같은 함수들 생성자로 넣어서 간소화
cpp이면 원형큐도 2배씩 공간늘리는 방식 사용 가능해보임<- 잘못생각함 원형큐를 쓰는 이유가 고정크기에 재할당 없이 쓰려고

근데 책에서는 스택과 달리 큐 함수에서 print같은 거 쓰는데 헤더 분리하는게 맞나 싶음 iostream써야하는거 아닌가<-operator<<를 cpp에서 쓰는이유가 헤더파일에서 iostream 안쓰려고

에러메시지나 exit같은건 똑같이 throw catch로 넘기고 받아야할거같음, 근데 스택에서 비었을대는 outofrange를 썼는데 큐에서 가득찼을경우에는 뭘써야하지?
근데 throw만 쓰면 큐는 스택과 달리 가득찬상태에서 계속 넣으려하는거 아닌가?
<- 잘못생각함 throw는 그 시점 enqueue 한 번만 중단 큐가 알아서 재시도하지 않고 재시도할지 말지는 try랑 catch쪽에서 결정

c에서는 함수의 매개변수로 queueType이라는 포인터를 보내서 관리하던데 cpp는 차피 클래스단위로 관리하니까 그럴필요없어보임 매개변수로 const& T item만 넘기면 될듯
이것도 스택처럼 dequeue는 void peek를 T로 남기는게 맞나 생각중
```
    void enqueue(const T &item)
    {
        if (is_full()) throw std::out_of_range("큐가 포화상태");
        rear = (rear+1)%MAX_QUEUE_SIZE;
        data[rear] = item;
    }
```
미리 rear위치를 옮겼는데 template를 쓰면서 std나 클래git commit -m "feat: Queue 구현 +스를 복사하는 과정에서 복사생성 중 에러나면 rear는 빈공간 가리키게됨
data와 같은 자료형 temp같은걸로 미리 확인하고 오류없으면 rear위치 옮기기



```
이제 T는 int일 수도 있지만, std::string이나 네가 만든 클래스일 수도 있다. std::string의 복사 생성자가 하는 일은 단순 비트 복사가 아니라 내부적으로 new char[] 호출 — 힙에서 새 메모리를 할당하는 거다. 이 할당이 실패하면 bad_alloc을 던진다.

정리하면 원인은 세 가지가 겹친 거다:

C엔 애초에 예외(exception)라는 게 없다. 함수는 그냥 끝까지 실행되거나, 아예 프로그램이 죽는다("중간에 되돌아가기"라는 개념 자체가 없다).
C의 element는 항상 POD(Plain Old Data)였다. int, struct(멤버 전부 POD)는 복사가 곧 memcpy다. 실패라는 상태가 존재하지 않는다.
C++ 템플릿은 T가 뭐가 될지 미리 알 수 없다. 생성자·복사 생성자가 임의의 코드(힙 할당, 파일 열기, 뭐든)를 실행할 수 있고, 그 임의의 코드는 예외를 던질 수 있다.

그러니까 이 문제, 큐라는 자료구조 자체의 결함이 아니다. 원형 큐 알고리즘(front/rear, wrap-around)은 C에서나 C++에서나 똑같다. 문제가 생긴 이유는 순수하게 "템플릿화해서 임의 타입을 받게 만든 결정" 때문이다. 만약 네가 template<typename T> 안 쓰고 CustomQueue를 int 전용으로 고정해서 만들었으면, dequeue()가 지금 순서 그대로여도 아무 문제 없다 — int 복사는 절대 안 던지니까.

이게 왜 Critical 4(템플릿 미적용)가 스택 리뷰에서 중요한 이슈였는지 다시 보여주는 지점이다: 제네릭하게 만드는 순간, 원본 C 코드엔 존재하지도 않았던 새로운 카테고리의 버그(예외 안전성)가 통째로 열린다. STL이랑 공정하게 비교하려면 템플릿화는 필수인데, 그 대가로 이 문제를 떠안는 거다 — 공짜가 아니다.

커널 레벨 연결점도 하나 짚고 가자. 리눅스 kfifo가 지금도 C로 짜여 있는 이유 중 하나가 이거다. kfifo는 항상 고정 크기 POD 바이트 덩어리(memcpy 기반)만 다루기 때문에, 지금 네가 겪는 "복사 도중 예외" 문제 자체가 원천적으로 존재하지 않는다. C++로 제네릭 컨테이너를 만드는 순간 떠안게 되는 이 복잡도를, 커널은 애초에 "임의 타입을 안 받는다"는 설계로 회피해버린 거다. 이건 트레이드오프지 어느 쪽이 무조건 우월한 게 아니다 — 네 보고서에 넣을 만한 포인트다.
```
"절대 실패 안 하는 연산(int 대입=커밋)"은 "실패할 수도 있는 연산(T 복사/이동)"보다 항상 나중에 실행돼야 한다.

스택과 동일하게: void dequeue() + peek() 분리
peek을 만든 이유가 사실상 없다고 봄 근데 이렇게 하면 일관성 지켜지면서 안정성도 생김

placement new? 이번에 알게도도니것


스택과 달리 헤더파일에서 간단한 출력문 만들예정 
iostream쓰면 무거워서 헤더파일 분리하는 이유없음
opretor로 출력 구현

0803
위험성 커밋이 작동한뒤에 확정넣어야ㅏㅎㅁ
unique_ptr이라는게 존재 자동으로 delete해줌
근데 지금은 한번 throw catch로 구현


<ostream>으로 헤더파일에서 출력구현한것과 별개로 .cpp 실제 구현부에서 자료형에 맞는 operator<< friend로 추가해야함
제네릭 컨테이너를 쓸 때 원소 타입한테 요구하는 조건(constraint)


지금은 매크로썼는데 나중에 실무에서는 매크로를 남발하면 똑같은 이름일때 문제 생길거같음
// 방법 1: constexpr — 스코프 있음, 타입 있음
class CustomQueue {
    static constexpr int CAPACITY = 5;  // 이 클래스 안에서만 유효, CustomQueue::CAPACITY로 명시 가능
};

// 방법 2: non-type template parameter — 인스턴스마다 다른 크기도 가능
template <typename T, std::size_t Capacity = 50>
class CustomQueue { ... };

근데 만약 똑같은 거 2개가 필요한게 아닌 객체의 소유권만 바꾸는 or 이전하는 경우는 하나하나 복사할 필요가 없지 않나? 그냥 포인터 새로 붙이고 원래서 nullptr로 없애면 되지않나